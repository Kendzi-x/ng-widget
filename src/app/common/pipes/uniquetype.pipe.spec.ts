import { UniquetypePipe } from './uniquetype.pipe';

describe('UniquetypePipe', () => {
  it('create an instance', () => {
    const pipe = new UniquetypePipe();
    expect(pipe).toBeTruthy();
  });
});
